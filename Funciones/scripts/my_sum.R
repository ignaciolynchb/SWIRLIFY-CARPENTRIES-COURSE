#No es necesario que realices cambios en este script, simplemente analizalo con atención

#my_sum es el nombre que recibe la función
#a y b son los inputs que recibe la función. Se encuentran entre paréntesis
my_sum <- function(a, b) { 
  the_sum <- a + b #aquí se escribe el cuerpo de la función
                   #en este caso, se almacena en the_sum el valor resultante de hacer a+b
  return(the_sum) #por ultimo, el return statement especifica a la función cuál es el output, en este caso, the_sum
}

#Escribe submit() para continuar